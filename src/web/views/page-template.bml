<h1 class="text-2xl font-bold mb-3">{title}</h1>
<p class="mb-4 opacity-70">This page's markup lives in <code class="badge badge-ghost">page-template.bml</code>, compiled to the same Hiccup at load.</p>
<ul class="list-disc ml-6 space-y-1">
  <li :for={p <- points}>{p}</li>
</ul>
